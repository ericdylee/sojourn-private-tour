# 프로젝트: Sojourn Korea — 부산 프라이빗 투어 마케팅

sojournkorea.net의 `/private-tour` 랜딩으로 트래픽과 예약 문의를 만드는 콘텐츠 생산 저장소.
코드 제품이 아니라 **마케팅 산출물**(카드뉴스·블로그·SNS·릴스)이 결과물이다.

## 캠페인 고정 조건

- 대상: **부산** 프라이빗 투어 (서울은 후순위)
- 콘텐츠 언어: **영어 단독** — 사이트가 전면 영문이라 랜딩 경험과 끊기면 안 된다
- 블로그: 자사 사이트 게재, **구글 SEO**, 3,000자(영문 공백 포함) 내외
- 카드뉴스: **1080×1080 × 6장**
- SNS: Threads / LinkedIn / X 각 500자
- 랜딩: **`https://www.sojournkorea.net/private-tour`** (`www.` 포함. apex는 308 리다이렉트)
- 마스코트: 부기(Boogi). 저작권자 부산광역시. **공공누리 제4유형 — 상업적 이용·변형 금지, 서면 승인 필수**

## 하네스: 부산 투어 마케팅

**목표:** 브리프 하나에서 카드뉴스 6장 · 블로그 · SNS 3편 · 릴스를 일관된 메시지로 생산한다. **팩트 원장(`_workspace/01_brief.json`)은 사람이 소유한다** — 이걸 쓰는 에이전트는 없다.

**세션 시작 시:** `docs/PRD.md`의 "현재 상태" 절을 먼저 읽고, 그 안의 "다음 작업" 인용 블록에 적힌 항목을 **사용자에게 먼저 알려라.** 진행 상황·다음 작업·대기 중인 사용자 액션·제약이 전부 거기 있다.

**트리거:** 캠페인 제작·수정·재실행 요청 시 `busan-campaign` 스킬을 사용하라. 산출물 하나만 필요하면 해당 전용 스킬(`cardnews-render`, `longform-blog`, `social-posts`, `brand-qa-check`, `reels-produce`)로 바로 가도 된다. 브랜드 규칙 질문은 `sojourn-brand-system`. 단순 질문은 직접 응답 가능.

**웹 콘솔:** 같은 에이전트를 브라우저에서도 돌릴 수 있다 — `cd web && npm start` → `http://127.0.0.1:4173`. 콘솔은 규칙을 복제하지 않고 `settingSources:['project']`로 이 하네스를 그대로 로드하므로, **에이전트·스킬을 고치면 콘솔도 같이 바뀐다.** **한 번에 에이전트 한 명만** 돌린다 (5인) — 전체 캠페인 일괄 실행 버튼은 없다. 사용자가 승인해야 하는 지점은 `mcp__gate__ask_*` 툴로 화면에 카드가 뜬다. 상세는 `web/README.md`.

**변경 이력:**
| 날짜 | 변경 내용 | 대상 | 사유 |
|------|----------|------|------|
| 2026-08-03 | 초기 구성 — 에이전트 6 + 스킬 8 (브랜드 시스템 + 오케스트레이터 포함) | 전체 | - |
| 2026-08-03 | 랜딩 URL을 `/tour.html`로 확정, 가격 언급 금지 규칙 추가 | brand-system, campaign-brief, brand-qa-check, campaign-strategist | `/private-tour` 404 확인 · 랜딩에 가격 표기 없음 |
| 2026-08-03 | 부기 라이선스 상태 기록 (부산 소재 소상공인 = 무료 대상, 승인 신청 예정) | brand-system/references | 사용자 확인 |
| 2026-08-03 | 부기 가이드라인 전면 재작성 — 공공누리 4유형, 반전·왜곡·AI생성 금지, 저작권 표기 필수, 공식 응용동작 카탈로그 | brand-system, cardnews-render, brand-qa-check, reels-produce, busan-campaign, card-producer, reels-producer | 부산시 공식 가이드북 2종 입수. 기존 규칙 다수가 부정확했음 |
| 2026-08-03 | 렌더러에 부기 라이선스 자동 검사 추가 (반전·비율왜곡·크레딧 누락) | cardnews-render/scripts | 사람이 눈으로 놓치는 위반을 기계가 막는다 |
| 2026-08-03 | 랜딩 URL을 `www.sojournkorea.net/private-tour`로 확정 | brand-system, campaign-brief, brand-qa-check, campaign-strategist | 사용자가 페이지 경로 변경 완료 |
| 2026-08-03 | 첫 캠페인 세트 생산 (부기 미등장 6장) + `.no-char`·`.with-head` 아키타입 변형 추가 | _workspace, output/cards, cardnews-render/assets | 라이선스 승인 전 카피·레이아웃 방향 검증 |
| 2026-08-03 | docs 4종(PRD·ARCHITECTURE·ADR·UI_GUIDE) 템플릿 → 실제 내용으로 작성 | docs/ | 기획 내용이 대화에만 있어 새 세션에서 유실됨 |
| 2026-08-03 | 사진 파이프라인 추가 — 매니페스트 원장 + `.photo-*` 트리트먼트 + 렌더 출처 검증. ADR-010(장소 AI 금지)·011(OpenAI 미도입) | assets/photos, cardnews-render, brand-system, UI_GUIDE, ADR | 카드에 이미지가 0장이라 레퍼런스 대비 빈약. 장소 이미지 AI 생성은 약속 허위표시 위험 |
| 2026-08-03 | 촬영 리스트 작성 (우선순위 A/B/C) | docs/SHOTLIST.md | 실사진이 유일한 차별 자산 |
| 2026-08-03 | 렌더러에 **외관 검사 2종 추가** — 줄수(allowlist·Range 기반) + 대비(글자 제거 스크린샷 픽셀 측정). NOTES/ISSUES 분리. ADR-014 | cardnews-render/scripts, SKILL, ADR | QA T1. 기존 검사가 전부 기하·메타데이터라 줄바꿈과 대비를 구조적으로 못 잡았고, 두 건(카드 06 URL 단어중간 줄바꿈 · 카드 02 흰글자 on 흰배경)이 exit 0을 통과했다 |
| 2026-08-03 | 카드 06에 페이저 `06 / 06` 추가 | _workspace/03_cards.html, output/cards | QA m5. 6장 중 5장만 인덱스가 있어 규칙이 아니라 누락으로 읽혔다 |
| 2026-08-03 | **부기 캐릭터 사용 보류 확정** — 캐릭터 없이 간다 | 전체 | 사용자 결정. 라이선스 승인 절차·고해상도 원본 확보 부담 대비 실익이 낮음 |
| 2026-08-03 | 블로그 1편 + SNS 3편 생산 | output/blog.md, output/social.json | 카드에 이어 나머지 채널 착수 |
| 2026-08-03 | 카드 03 "Start at nine. Or noon." 제거 → "You set the order." | _workspace/01_brief.json, 03_cards.html, output/cards | QA B2. 랜딩에 시각 표기가 전무한데 브리프가 운영시간을 주장했다. 브리프 원장 오류였음 |
| 2026-08-03 | 브리프 `banned_extra`에 "투어의 시작·종료 시각" 추가 | _workspace/01_brief.json | 원장을 안 고치면 다음 실행에서 같은 문구가 재생성된다 |
| 2026-08-03 | 카드 액센트 1종·15% 상한 위반 교정, 카드 02 페이저 소실 복구, 카드 06 CTA 크롭가드 이탈 | cardnews-render/assets/brand.css, output/cards | QA MAJOR 4건. 수정이 공용 CSS에 들어가 이후 전 세트에 적용된다 |
| 2026-08-03 | QA 수정분 실측 검증 — 6장 전부 단일 액센트·15% 이하(04: sand 7.89 단독, 06: coral 8.69 단독) | output/cards | 육안이 아니라 픽셀로 확인. **재검수는 아직 안 돌았다 — 공식 판정은 여전히 HOLD** |
| 2026-08-03 | 개념 이미지 생성에 Codex 빌트인 `gpt-image-2` 도입. ADR-012가 ADR-011을 대체 | ADR, PRD, assets/photos | API 키·과금·코드 추가 없이 셸 호출로 되고, Higgsfield 잔액 8크레딧을 릴스용으로 아낀다 |
| 2026-08-03 | 카드 03에 개념 이미지(바늘 없는 시계) 추가 | _workspace/03_cards.html, assets/photos | 카드 06장 중 이미지가 1장뿐이라 빈약했다 |
| 2026-08-03 | **ADR-013 신설 — 이미지도 원장에 없는 사실을 주장 금지.** QA에 A12 항목 추가 | ADR, brand-qa-check | 생성된 시계가 지시하지 않은 9시 10분을 그려, 문장으로 지운 운영시간 주장(B2)이 그림으로 되살아났다. ADR-010(장소)만으로는 못 잡는다 |

| 2026-08-04 | **블로그 첫 편 발행 — 라이브.** 사이트 레포 `~/PROJECTS/sojourn-relocation-v1`에 커밋·푸시 | 사이트 레포 | 사용자가 `/blog` 섹션 신설·배포 완료 |
| 2026-08-04 | 블로그 생성기에 `faq:` 프론트매터 → FAQPage 구조화 데이터 지원 (테스트 7종) | 사이트 레포 `scripts/publish.js` | 생성기가 Article만 내보내 FAQ 리치결과를 못 받았다. 답변이 본문에 없으면 빌드가 실패한다 |

| 2026-08-04 | 블로그 히어로 이미지 제작 — 개념 이미지(추상 루트 라인 + 새벽→밤 그래디언트) | assets/photos, 사이트 레포 | 히어로가 없어 og:image가 로고로 나갔다. 실제 장소 AI 생성은 ADR-010으로 금지라 논지를 그리는 도식으로 갔다 |

| 2026-08-04 | **블로그 형식 전환 — 한 편 = 한 장소, 매거진 기사체.** 첫 글·추상 히어로 폐기, 옛 URL은 301 | 사이트 레포 | 첫 글은 물류를 논증했을 뿐 장소를 묘사하지 않았다. 사람들이 검색하는 건 동선 최적화가 아니라 장소 이름이다 |
| 2026-08-04 | 장소 사진은 **실사진·라이선스 사진**으로 간다. ADR-010은 AI 생성 금지이지 실사진 금지가 아니었다 | assets/photos, manifest | 제약을 과하게 읽고 추상 도식으로 우회했던 것을 바로잡음 |
| 2026-08-04 | 팩트 규칙 2분화 — **장소 사실은 출처 표기, 서비스 사실은 원장** | 전체 | 매거진 기사에는 장소의 역사·주소·개장시간이 필요한데 원장에는 없다. 성격이 다른 사실이다 |
| 2026-08-04 | 블로그 본문 figure/figcaption 지원 추가 | 사이트 레포 templates/_head.html | CC 라이선스는 사진마다 출처 표기가 의무다 |

| 2026-08-04 | 블로그 제목에 `\|` 브레이크 마커 도입 — h1만 적용, lg(1024px) 이상에서만. 테스트 8종 | 사이트 레포 scripts/publish.js | `text-balance`가 "Haedong Yonggungsa: The / Temple..."로 의미를 끊었다. md에서 강제하면 마지막 단어가 고아가 돼 lg로 올렸다 |

| 2026-08-04 | 블로그 2편 발행 (해동용궁사·감천문화마을) + 사진 9장 원장 등재 | 사이트 레포, assets/photos/place | 장소별 매거진 형식 확립. 감천 편이 최신 형식 원본 |
| 2026-08-04 | **장소 사실은 출처 표기, 서비스 사실은 원장** 원칙을 실제 2편에 적용 | 블로그 | 창건연도가 학계에서 다투어지면 그 사실까지 쓴다. 제3자 가격(스탬프 지도)도 숫자는 안 쓴다 |

| 2026-08-05 | 블로그 목록을 가로형 카드 세로 스택 + 페이지네이션(8편/쪽)으로 개편. 카테고리 칩 stretch 버그 수정 | 사이트 레포 | 최신 1편만 피처 카드라 2번째 글이 "덜 중요한 글"로 보였다 |
| 2026-08-05 | 제목에 `\|` 브레이크 마커 (h1 한정, lg 이상) | 사이트 레포 | `text-balance`가 의미 단위를 무시하고 줄을 나눴다 |
| 2026-08-05 | **웹 콘솔 `web/` 신설** — Claude Agent SDK(`settingSources:['project']`)로 하네스를 복제 없이 로드. 실행·라이브·산출물·브리프·이력 5개 뷰. 로컬 전용(127.0.0.1), 동시 실행 1건, 실행 전 스냅샷 | web/ | 터미널 없이 팀을 돌리고 승인 지점을 화면에서 처리하기 위함. 규칙을 콘솔에 복제하면 두 군데가 갈라지므로 하네스를 그대로 로드하는 쪽을 택했다 |
| 2026-08-05 | 콘솔에 **승인 게이트 MCP 서버** 추가 (`ask_choice`·`ask_approval`·`ask_text`) | web/lib/gate.mjs | 헤드리스 에이전트는 "사용자에게 물어보라"는 지시를 받아도 창구가 없어 임의로 답을 정하고 진행한다. 툴 호출을 서버에서 블로킹해 사람 응답을 tool_result로 돌려준다 |
| 2026-08-05 | **콘솔에서 전체 캠페인 일괄 실행 모드 제거 — 한 번에 에이전트 한 명.** 에이전트별 프리셋 추가 | web/ | 사용자 결정. 원장을 다시 쓰고 4채널을 무인으로 갈아엎는 파이프라인은 틀렸을 때 가장 비싸고 중간에 멈추기 어렵다. 오케스트레이터 스킬은 하네스에 남아 지시문으로는 여전히 호출 가능 |
| 2026-08-05 | **`campaign-strategist` 제거 → 에이전트 6인에서 5인으로. 팩트 원장 소유권을 사람에게 이관** | agents 5종, campaign-brief, busan-campaign, web/ | 사용자 결정. ① 8/4 블로그 형식 전환("한 편 = 한 장소")으로 캠페인 레벨 앵글에서 파생된 첫 글이 폐기됐다 ② 팩트 규칙이 둘로 갈라져(장소=출처 표기 / 서비스=원장) 원장이 덮는 범위가 줄었다 ③ 콘솔에 검증 붙은 원장 편집기가 생겼다. 게다가 원장이 틀린 유일한 사례(QA B2 운영시간)가 전략가 자신의 오류였다 |
| 2026-08-05 | 제작자 4인의 "`campaign-strategist`에게 물어라" 경로를 **사용자 질의**로 교체. `campaign-brief` 스킬을 **원장 계약서**로 성격 변경 (에이전트 작성 금지 명문화) | agents, campaign-brief | 전략가가 사라졌으니 팩트 질의가 갈 곳이 없었다. 기본값은 여전히 "답이 없으면 쓰지 마라" |
| 2026-08-05 | 콘솔 브리프 화면에 **원장 생성** 기능 추가. 새 원장은 표준 UNVERIFIED 3건 + 금지 표현 5건을 갖고 시작 | web/lib/brief.mjs, web/public/views/brief.js | 전략가가 첫 원장을 만들던 자리를 사람이 이어받았다. 백지에서 시작하면 QA가 이미 막은 구멍이 다시 열린다 |
| 2026-08-05 | **콘솔 보안 2건 수정** — ① 변경 요청에 CSRF 토큰 필수 ② 경로 가드를 realpath 기반으로 교체. 회귀 테스트 8종 추가 | web/server.mjs, web/lib/paths.mjs, web/public/app.js, web/test/ | 커밋 후 보안 검토에서 발견, 둘 다 재현 확인. ① 루프백 바인딩은 브라우저를 막지 못한다 — `<form enctype="text/plain">`은 프리플라이트 없이 나가고 `POST /api/runs`는 공격자 지시문으로 에이전트를 띄운다(드라이브바이 RCE) ② `path.resolve`는 `..`만 정규화하고 심볼릭 링크는 따라간다. `output/` 안의 링크로 `/etc/hosts`가 실제로 서빙됐다 |
| 2026-08-05 | **두들(손그림 라인 마크) 시스템 신설** — `.doodle` + 인라인 SVG. 렌더러가 블리드 허용, 락업·페이저 침범은 차단 | brand.css, brand-system §5-2, render-cards.mjs | reference2의 흰 라인 드로잉 레이어가 우리 카드에 0개였다. `currentColor`라 액센트 예산을 안 쓰고, 장소·가격·시각을 주장하지 않아 ADR-013 대상도 아니다 |
| 2026-08-05 | 타입 스케일에 `d-xxl`(250px) 추가 — **커버 전용, 8자 상한** | brand.css, brand-system §2 | 9자부터 접히고 접힌 초대형 워드는 그냥 큰 글씨다. 커버 첫 단어에만 쓴다 |
| 2026-08-05 | flex 아키타입의 타입 박스를 `align-self:flex-start`로 shrink-wrap | brand.css | 컬럼 flex에서 `.display` 박스가 글자 폭과 무관하게 프레임 우측 끝까지 늘어나, 시각적으로 빈 절반에 놓은 사진이 충돌로 잡혔다(카드 06). 검사가 박스를 보므로 박스가 실제 그려진 것을 서술해야 한다 |
| 2026-08-05 | **카드 세트 v2 생산 — reference2 룩 테스트.** 카피는 v1 고정, 비주얼만 교체. v1은 `_workspace/cards_v1/`·`03_cards_prev.html`로 보존 | _workspace, output/cards | 카피와 룩을 한 번에 바꾸면 두 세트가 비교 불가능해진다. 바뀐 변수는 비주얼 언어 하나뿐 |
| 2026-08-05 | 카드 04·06에 실제 장소 사진(해동용궁사) 투입 — PD / CC BY 2.0만 사용 | _workspace/03_cards.html | 이미지가 2/6뿐이라 레퍼런스 대비 빈약했다. 매니페스트의 CC BY-**SA** 건은 상업 카드에 동일조건변경허락 의무가 붙어 제외했다 — 사진의 정직성과는 다른 문제다 |
| 2026-08-05 | **액센트 면적은 도형이 아니라 렌더 픽셀로 판정한다** | _workspace/03_cards.html 주석 | `.t-box`의 판 넓이(11.0%)로 타입 크기를 제한하려 했는데, 흰 글자가 판을 덮어 실제 코랄은 3.37%였다. 바운딩 박스 기준은 과대추정이다. v2 실측: 01 코랄 3.37 · 02 코랄 2.21 · 03 코랄 5.87 · 04 샌드 5.82 · 05 샌드 3.02 · 06 코랄 9.24 — 6장 전부 단일 액센트·15% 이하 |
| 2026-08-05 | **배경 사진 시스템 신설 — `.photo-bg`.** 콜라주 사진과 분리하고, 충돌 검사 앵커에서만 제외 | brand.css, brand-system §5-1, render-cards.mjs | 사진이 카드 배경 전체가 되면 타입이 사진 위에 오는 게 정상인데, `.photo`는 "타입이 피해야 할 물건"이라 렌더러가 전부 실패로 잡았다. 원장·슬롯·AI금지·크레딧 검사는 그대로 적용된다 |
| 2026-08-05 | 스크림(`.scrim-*`)·웨이브(`.wave`)·`.t-plate`·`.pill`·`.lay-photo`·`.on-light` 추가 | brand.css, brand-system §1·§3·§5-1 | 사진 위 가독성을 소프트 섀도 없이 확보해야 했다. 스크림이 대비를 사고 `.t-plate` 스트로크가 디테일에서 글자를 떼며, 본문·리스트는 흰 웨이브에 올린다. 스크림은 §1 그라데이션 금지의 유일한 예외로 명시 |
| 2026-08-05 | 프레임 이탈 검사에 **클리핑 조상 예외** 추가 | render-cards.mjs | `overflow:hidden` 안에서 확대한 배경 이미지는 한 픽셀도 프레임 밖에 그려지지 않는데 이탈로 잡혔다. `.card` 자신은 제외해야 검사가 무력화되지 않는다. 클리핑 조상은 같은 스윕에서 따로 검사된다 |
| 2026-08-05 | **카드 세트 v3 — 사진이 배경 전체.** 전체 = reference2, 디테일 = ref1(클룩). 이미지 6/6. v2는 `_workspace/cards_v2/`에 보존 | _workspace, output/cards | 사용자 지시. v1(이미지 2/6) → v2(4/6, 콜라주) → v3(6/6, 풀블리드) |
| 2026-08-05 | 카드 02 배경 재생성 (`concept/coach-aisle.png`, gpt-image-2) + 구 `concept/crowded-bus.png` **삭제** | assets/photos, manifest, 03_cards.html | 사용자가 못 쓴다고 판정. 구본은 흰 여백이 구워져 있어 CSS 확대로 억지 풀블리드를 만들어야 했고 해상도가 낮았다. 신본은 처음부터 배경용 — 프레임을 채우고 타입이 올라갈 상단 1/3을 비웠다. **v1·v2 html이 삭제된 파일을 참조하므로 재렌더가 깨진다** (렌더된 PNG는 `_workspace/cards_v1·v2/`에 남아 있고, 실패는 조용하지 않고 크게 난다) |
| 2026-08-05 | **ADR-015 신설 — 배경은 "그 카드가 장소를 주장하는가"로 갈린다.** 주장하면 실사진, 무드만 필요하면 concept AI 허용 | ADR, manifest, 03_cards.html | 사용자 결정. ADR-010의 금지 단위가 파일이 아니라 카드였다 — 같은 해안 사진도 `IN BUSAN` 밑이면 주장이고 `TELL US YOUR DAY` 밑이면 아니다. 무드컷 생성 시 랜드마크(사찰 지붕선·현수교·도색 마을·스카이라인)를 프롬프트에서 명시 배제 |
| 2026-08-05 | 카드 03·04·06 배경을 gpt-image-2 무드컷으로 교체 (`open-road` · `coast-road-branch` · `sea-blue-hour`) | assets/photos, manifest, 03_cards.html | 라이선스 차단 카드가 3장 → **1장(01 감천)**. 화질도 위키미디어 스냅샷보다 크게 낫다. **대가: 생성컷은 한국처럼 안 보인다** — 부산처럼 보이면 ADR-010 위반이라 익명성이 조건이다 |
| 2026-08-05 | **카드 세트 v4 — SIMPLE 방향 전환.** `.lay-simple` 신설: 사진 풀블리드(원색) + 검정 스크림 + Inter 800 88px 2줄 + 하단 1줄 + 배지 + 세로 핸들. Anton·두들·웨이브·코랄판 전부 미사용 | brand.css, UI_GUIDE, _workspace | 사용자 지시(레퍼런스 캐러셀 3종). 세 규칙을 `.lay-simple` 안에서만 유예 — 초대형 워드, 블루 지배, 소프트 섀도 금지. 사유는 brand.css SIMPLE 블록 |
| 2026-08-06 | **만원 버스 개념 이미지를 폐기한다 — 다시 만들지 마라.** 원장 등재·파일·생성 캐시 전부 제거 | assets/photos, manifest | 사용자 판단: 마케팅 재료로 못 쓴다. 두 번 시도했고(higgsfield → gpt-image-2) 두 번 다 폐기됐다. "단체 투어의 문제"를 그림으로 말하려면 **경쟁 상품을 부정적으로 묘사**해야 하는데, 그건 "현지에 사는 사람의 목소리"라는 톤과 충돌하고 브랜드가 서 있고 싶은 자리가 아니다. 대안은 우리 쪽을 긍정적으로 보여주는 것이다 |
| 2026-08-06 | **카드뉴스 절차를 4단계로 재설계** — 주제 선정 → 캐러셀 개별 기획 → 이미지 선별 → 생성. 각 단계가 `_workspace/02_carousel.json`에 누적된다 | cardnews-render/SKILL.md, assets/carousel-plan.example.json | 사용자 지시. 단계별 파일을 남기는 이유는 격식이 아니다 — 3단계에서 사진이 없다는 게 드러나면 2단계로 돌아가야 하는데, 기획이 대화에만 있으면 전부 다시 해야 한다 |
| 2026-08-06 | **1단계에 사진 재고 게이트 신설** (착수 3조건: 코스 O · 팩트 출처 O · 사진 5장↑) | cardnews-render/SKILL.md, card-producer | 이번 세션에서 실제로 겪었다. 문구를 먼저 짜고 사진을 나중에 찾으니 잘 쓴 기획을 통째로 버렸다. 재고를 먼저 세면 가진 것 안에서 시작한다 |
| 2026-08-06 | **카드 팩트 출처 규칙 명문화** — 카드에는 출처를 달 자리가 없으므로, 장소 사실은 **그 장소 블로그에 이미 출처와 함께 발행된 것**만 쓴다 | cardnews-render, card-producer, busan-campaign | 카드는 회수가 안 되는데 출처를 못 단다. 제3자 가격·운영시간·소요시간은 참이어도 카드에 안 올린다 |
| 2026-08-06 | 오케스트레이터 Phase 2의 화살표 방향 수정 — **blog-writer → card-producer(장소 사실+출처)가 먼저** | busan-campaign | 위 규칙의 필연적 결과. 블로그가 책임지지 않은 사실을 카드가 먼저 쓸 수 없다 |
| 2026-08-06 | 드리프트 3건 제거 — Phase 0의 부기 자산 선행조건, 릴스의 부기 라이선스 게이트, `card-template.html`의 Anton+부기 골격 | busan-campaign, cardnews-render/assets, card-producer | 부기는 2026-08-03에 사용 보류 확정됐는데 오케스트레이터가 여전히 누끼 PNG를 선행 조건으로 요구해 실행이 그 앞에서 막혔다. 구 템플릿은 복사하면 폐기된 디자인이 되살아나는 함정이었다 |
| 2026-08-05 | 헤드라인·지지문 서체를 **Montserrat**(`--voice`)로. 고정 요소는 Inter 유지 | brand.css, UI_GUIDE | 사용자 요청(영어권 마케터 상용 + 가독성). Poppins보다 x-height가 커서 사진 위 대형 글자에서 유리하다. 폭이 넓어 88→80px로 낮췄고, x-height 덕에 작아 보이지 않는다 |
| 2026-08-05 | **액센트 `--orange #FF7A29` 신설 — `.lay-simple` 안에서 coral을 대체.** 배지도 오렌지 바탕 + ink 글자 | brand.css, UI_GUIDE | 액센트가 **글자**로 들어가는 방향이라 휘도가 결정적이다. 검정 스크림 대비 coral 3.07:1 vs orange 4.03:1. 감천 사진은 파란 지붕이 지배해 오렌지가 보색이다. 배지는 흰 글자 2.6:1이라 ink(8.1:1)로 갔다 |
| 2026-08-05 | `.scrim-soft` 상단 램프를 22% → **35%까지 최대치 유지** | brand.css | 오렌지는 휘도 0.35로 중간이라 흰 글자와 달리 얇은 스크림에서 사라진다. 카드 02 둘째 줄이 **1.18:1로 실패**했다. 흰 글자 기준으로 잡은 스크림은 컬러 액센트를 못 버틴다 |
| 2026-08-05 | **주제 단위를 상품 → 장소로 전환.** 캠페인 `gamcheon-culture-village`, 감천 6장 | _workspace/03_cards.html | 블로그가 이미 "한 편 = 한 장소"로 갔다. 카드도 같은 단위로 맞춘다. 오퍼는 6번 카드에서 한 번만 |
| 2026-08-05 | 카드 카피의 출처를 **자사 블로그 발행본**으로 잡음 | 03_cards.html | 감천 팩트는 이미 `2026-08-04-gamcheon-culture-village.md`에 출처와 함께 발행돼 있다. 카드에 출처를 못 다는 대신 **이미 우리가 공개적으로 책임진 문장만** 쓴다. 무료 입장·안내소 운영시간·스탬프 지도 가격은 의도적으로 제외 — 카드에 출처를 달 자리가 없고 바뀌는 값이다 |
| 2026-08-05 | **정정: 감천 사진 저작자 표기는 깨진 게 아니었다.** Flickr 계정명이 실제로 `bryan...` (ID 91049143@N00) | manifest, PRD | 커먼즈 파일 페이지로 확인. 전 턴에 "CC 요구 저작자 표시 불가"라고 한 것은 오류다. 남는 쟁점은 **동일조건변경허락 하나** — 사진 위에 타입을 얹으면 2차적저작물이라 카드 자체가 CC BY-SA 2.0으로 나가야 한다 |
| 2026-08-05 | **사진 라이브러리가 카드의 상한이 됐다** — 배경 6장 중 3장이 CC BY-SA, 감천 2건은 저작자 표기가 `bryan…`으로 잘려 CC 요구를 못 채운다 | PRD, SHOTLIST | 사진이 배경 전체가 되면서 라이선스가 스타일 문제에서 발행 차단 사유로 승격됐다. 자사 실사진이 유일한 해소책 |
| 2026-08-06 | 콘솔 이름을 **Sojourn 콘텐츠 자동화**로 변경 | web/ (탭 제목·로고 대체텍스트·실행 h1·시작 로그) | 사용자 결정. "캠페인 콘솔"은 어떤 화면인지를 가리켰지 무엇을 하는 도구인지를 가리키지 않았다. 팀에 소개할 이름이 필요해졌다 |
| 2026-08-06 | **`ericdylee/카드뉴스` 브랜치를 main에 병합** — 카드 v4(SIMPLE)·4단계 절차·ADR-015가 main으로 들어왔다. 충돌 3파일 수동 해소 | CLAUDE.md, docs/PRD.md, card-producer | 두 갈래가 각자 진행됐다 — main은 전략가 제거(5인 전환), 브랜치는 카드 절차 재설계. 둘 다 살려야 해서 기계 병합이 불가능했다. 병합 중 `card-producer.md`에 없어진 `campaign-strategist`에게 물어보라는 **살아 있는 지시**가 남아 있어 사용자 질의로 교체했다 |
| 2026-08-06 | PRD "현재 상태"의 모순 정정 — v3와 v4가 둘 다 `output/cards/`를 자기 것이라 주장했다. v4가 현재, v3는 `_workspace/cards_v3/` | docs/PRD.md | 양쪽 브랜치가 각자 "현재"를 갱신한 결과. 새 세션이 이 절을 단일 출처로 읽으므로 모순이 그대로 전파된다 |
| 2026-08-06 | 카드 05 헤드라인 교체 — "It is stairs, end to end" → **"Even the easy way / is uphill"** | _workspace/03_cards.html, 02_carousel.json, output/cards/05.png | 사진에 계단이 한 칸도 없어 **사진이 문장을 반증**했다. 문장 자체는 블로그 발행본의 참인 서술이지만, 하필 그 사진이 그 문장의 **예외**를 찍은 컷이다 — 블로그가 같은 사진에 붙인 캡션이 "Signed paths run along the ridge; almost everything else is stairs."다. 사진을 문구에 맞추는 대신 문구를 사진에 맞췄다. 감천 계단 촬영 요청은 유효하게 남긴다 |
| 2026-08-06 | **콘솔 첫 실사용 검증** — 카드 부분 수정을 콘솔로 완주. `ask_approval` 게이트의 실제 에이전트 호출을 처음 확인 | web/, docs/PRD.md | 지금까지 콘솔로 카드를 한 번도 안 만들어봤다. 실사용에서 결함 2건이 나왔다: ① 승인 카드의 버튼이 근거가 길면 화면 밖으로 밀려 보이지 않는다 ② 탭을 닫으면 진행 중 실행과 화면이 끊겨 재접속 후 승인이 안 먹는다(API로 우회함). **앱으로 포장하면 창 닫기가 흔해지므로 ②가 특히 중요하다** |
| 2026-08-07 | **폰트를 로컬 woff2로 고정.** `brand.css`의 Google Fonts `@import` 제거 | assets/fonts, brand.css, lib/fonts.mjs | 네트워크가 실패하면 대체 글꼴로 렌더되는데 `document.fonts.ready`는 그래도 resolve해서 **렌더러가 틀린 결과를 성공으로 보고했다.** 카드는 사람이 한 장씩 봐서 견뎠지만 프레임 수백 장에는 통하지 않는다. 카드 6장 sha256 6/6 동일 확인 |
| 2026-08-07 | 대비·사진 검사를 `lib/`으로 추출 | cardnews-render/scripts | 릴스가 같은 검사를 쓴다. 613줄 단일 스크립트라 밖에서 못 썼다. 동작 불변 — 카드 재렌더 sha256으로 확인 |
| 2026-08-08 | **릴스 제작 자동화 구축 — 크레딧 0.** HTML 원본에서 9:16 재조판 → CSS 애니메이션 시점 seek → 프레임을 ffmpeg stdin으로 흘림. 씬 단위 해시 캐시. 검사 3종 신설(세이프에어리어·헤드라인 단어수·3프레임 대비) | cardnews-render/scripts, assets/reel.css, reels-produce, reels-producer, web/prompts | 릴스가 Higgsfield 크레딧에 막혀 있다고 기록돼 있었는데, 카드 재활용 릴스는 카드 렌더와 같은 도구로 전부 된다. 실사진 image-to-video는 크레딧 이전에 ADR-010 위반이다 — AI가 실재 장소의 없던 프레임을 그린다. 첫 편 실측: 1080×1920·30fps·22.5초·675프레임. 결정론 확인 — 캐시를 지우고 콜드로 재렌더한 mp4가 바이트 동일 |
| 2026-08-08 | **릴스 발행 게이트 — fail-closed.** QA 판정과 사진 권리를 스크립트가 직접 읽고, 통과를 증명 못 하면 `reel_INTERNAL.mp4` + 배너 | render-reel.mjs, reel-gate.mjs | 파이프라인은 QA가 HOLD여도 돈다. "내부 시안까지만"이 사람의 기억이 아니라 파일 자체의 성질이어야 한다 |
| 2026-08-08 | 릴스 검사에 **text-transform 검사** 추가 — 신규 검사 3종 → 4종, **총 8종**(기존 4 + 신규 4) | cardnews-render/scripts/lib/reel-checks.mjs | `reel.css`의 `.display { text-transform: none }` 리셋이 지워지면 brand.css의 카드용 대문자 규칙이 이겨 헤드라인이 약 23% 넓어지는데, 세이프에어리어도 단어수 검사도 이걸 못 잡는다 — 막히지 않고 틀린 채로 나간다. 이 파이프라인이 ffmpeg 합성 대신 CSS+Playwright로 렌더하는 이유가 "결과가 검사 가능하다"는 것인데, 조용히 나가는 결함은 그 논거가 못 덮는 유일한 경우였다 |
| 2026-08-08 | `SHOTLIST.md`에 **영상 클립 절 신설** | docs/SHOTLIST.md | 릴스 포맷의 천장은 동영상 소재가 없다는 데서 온다. 사진 찍으러 간 자리에서 5~10초 클립을 같이 찍으면 비용 없이 천장이 올라간다 |
| 2026-08-08 | **드리프트 제거 — 릴스 스킬·에이전트·콘솔 프리셋에 남아 있던 부기 선행조건과 크레딧 게이트를 걷어낸다.** `docs/PRD.md`·`docs/SHOTLIST.md` 갱신 | reels-produce, reels-producer, web/prompts, docs/PRD.md | 부기는 2026-08-03에 사용 보류가 확정됐는데 릴스 스킬·에이전트 본체엔 "라이선스 승인완료"·누끼 PNG 확보가 여전히 선행 조건으로 남아 있어, 그대로 돌리면 만족될 수 없는 조건 앞에서 멈췄다. 콘솔 프리셋의 "Higgsfield 크레딧 부족하니 생성 금지"도 이제 거짓이다 — 크레딧을 아예 안 쓴다. `_workspace/02_carousel.json`은 QA 3회차 교정분(`0fb1aa3`)을 승계하지 못해 여전히 낡았다 — 재동기화는 이 작업 범위 밖의 card-producer 몫으로 남겨 기록만 한다. PRD "다음 작업"의 Higgsfield 충전 항목도 같은 이유로 제거 — QA 재검수가 실제 다음 작업이다 |
| 2026-08-08 | **릴스 발행 게이트의 사각 봉합 — 씬 원장과 HTML이 다른 사진을 가리키면 ISSUE**(`checkPlanPhoto`) | render-reel.mjs, reel-checks.mjs, docs/PRD.md | 게이트는 `04_reel_plan.json`의 `photo`로 라이선스를 판정하는데 프레임에 찍히는 것은 HTML의 `<img data-photo>`였고, 둘을 맞춰 보는 코드가 없었다. `checkPhotos`에는 라이선스 규칙 자체가 없다(등재·슬롯·AI생성·크레딧만 본다). **재현: 씬 01의 `<img>`만 매니페스트의 고아 CC BY-SA 2.0 행으로 바꾸고 QA를 PASS로 두자 "발행 가능"·이슈 0건으로 발행용 `reel.mp4`가 나왔다** — 화면의 크레딧은 원장 쪽 저작자·라이선스를 그대로 표시한 채로. 고아 5건이 사용 중 파일과 한 글자 차이라 오타 거리다. PRD의 "렌더러가 쓰인 사진만 검사한다"는 서술도 정정 — 그건 `render-cards.mjs`에만 참이었다 |
| 2026-08-08 | **씬 캐시에 잘린 mp4가 앉는 경로를 막는다** — 인코더는 `<key>.mp4.part`에 쓰고 성공 시에만 rename, 캐시 히트는 ffprobe로 길이·프레임 수를 실측해 검증 | reel-capture.mjs, render-reel.mjs | ffmpeg은 SIGINT를 받으면 출력을 버리지 않고 **마무리한다.** 냉시작 4분 렌더 중 Ctrl-C — 이걸 멈추는 정상적인 방법 — 가 재생까지 되는 짧은 mp4를 정상 키 자리에 남겼고, `access()`만 보는 다음 실행이 `cache hit`으로 받아들였다. 실측: 4.5초 자리에 4.033초(121/135프레임)가 앉아 22.03초짜리 릴스가 나왔는데 요약 줄은 22.5초라고 적었다 — **그 줄이 `plan.total_ms`를 되뇌고 있어서 산출물과 구조적으로 어긋날 수 없었다.** 이제 최종 mp4도 실측해서 찍고, 원장과 다르면 파일을 지우고 실패한다. `-f mp4` 명시 필요(ffmpeg이 `.part` 확장자로는 먹서를 못 고른다). 인코더 출력은 바이트 동일 — RENDERER_VERSION 유지 |
| 2026-08-08 | **줄바꿈 검사를 릴스에 배선한다** — `.sub strong`(URL) 한 줄 강제 | reel-checks.mjs | `APPEARANCE.singleLine`은 카드 선택자 5종(`.cta-band .url` 등)인데 릴스는 그중 아무것도 그리지 않는다 — 가져와 봐야 "항상 아무것도 못 찾는 검사"다. ADR-014가 이 검사를 만든 계기(QA T1, 카드 06 URL 단어중간 줄바꿈)가 릴스에서는 배선조차 안 돼 있었다. 실측: 캠페인 경로가 붙은 URL이 두 줄로 접히는데 다른 검사 전부 0건 — 독자가 받아 적어야 하는 유일한 문자열이다. **오버플로 검사는 넣지 않기로 결정하고 사유를 코드에 남겼다** — `.type`이 `justify-content:flex-end`로 위로 자라 세이프에어리어가 먼저 걸린다(실측 여유 864px vs 200px 한계) |
| 2026-08-08 | **QA PASS는 발행 조건이지 착수 조건이 아니다** — 4개 파일의 상충 서술 통일 | reels-produce, reels-producer, busan-campaign, web/prompts | 같은 저장소가 "PASS 아니면 착수 금지"와 "HOLD면 INTERNAL이 정상"을 동시에 말했고, 콘솔 실행 한 번이 두 지시를 같이 읽었다. **fail-closed 게이트가 있는 이유가 HOLD 상태의 제작을 안전하게 만드는 것**이므로 착수를 막으면 사고가 아니라 작업이 막히고, 사람은 판정 파일을 고쳐 우회한다. 실제로 커밋된 릴스가 자기 스킬이 금지한 전제에서 만들어졌다 |
| 2026-08-08 | 릴스 스킬의 두 문장 교정 — "솔리드 컬러 바를 깔아라" → **`.scrim`을 걷어내지 마라**, "줄당 4~6단어" → **헤드라인 합계 6단어** | reels-produce/SKILL.md | 배송된 시스템의 대비 장치가 바로 그 반투명 스크림이고, `reel.css`가 램프 네 종을 실측해 고른 근거를 28줄로 적어 뒀다 — 71행을 따르는 에이전트는 그걸 뜯어낸다. 브랜드 시스템이 금지하는 것은 blur이지 반투명이 아니다. 단어 수도 같은 파일 49행·실제 검사(`MAX_HEADLINE_WORDS=6`)와 어긋났다(줄당 4~6이면 두 줄에 12단어) |
| 2026-08-08 | 릴스 산출물 문서를 실제 파이프라인에 맞춘다 — `scene_plan.md` 폐기(승인 대상은 `04_reel_plan.json`), `reel_INTERNAL.mp4` 명시, `caption.txt` 미작성 사실 명기 | reels-produce, reels-producer, busan-campaign, web/prompts | 두 문서가 한 번도 만들어진 적 없는 파일 두 개를 산출물로 약속했다. 씬 표를 두 곳에 쓰면 갈라지므로 렌더러가 실제로 읽는 원장 하나로 합쳤다. `caption.txt`는 요구를 유지하되 **첫 릴스에는 없다는 사실을 적었다** — 판정 HOLD·내부 시안이라 발행 대상이 아니었다. 없는 것을 있다고 적어 두면 다음 사람이 찾다가 만다 |
| 2026-08-08 | `reel-plan.mjs` — `fps` 타입 검증 추가, 씬 수 메시지를 실제 강제 범위(3~5)로 정정 | reel-plan.mjs | `fps`는 프레임 수 계산과 캐시 키 양쪽에 들어가는데 `duration_ms`와 달리 검사가 없었다 — `fps: 0`은 프레임 0장짜리 씬을, 문자열은 조용한 형변환을 만든다. 메시지는 "4~5개로 맞춰라"인데 코드는 3개를 통과시켰다(Task 5의 의도적 결정). 하한 3은 유지하고 문구만 사실에 맞췄다 — 안내와 강제가 다르면 파일이 어느 쪽이 강제인지 말해야 한다 |

## CRITICAL 규칙

- CRITICAL: 프로젝트 **루트에 package.json을 만들지 마라.** 이유: `.claude/settings.json`의 Stop 훅이 루트 package.json을 감지하면 존재하지 않는 `npm run lint/build/test`를 매 턴 실행해 실패한다. 렌더 의존성은 `.claude/skills/cardnews-render/scripts/`에 격리한다.
- CRITICAL: 브리프 `facts` 원장에 없는 가격·소요시간·운영시간을 산출물에 쓰지 마라. 투어는 실제 이행 약속이라 틀리면 환불·분쟁 사유다. **이건 글자만의 규칙이 아니다** — 이미지 속에 읽히는 시계 시각·가격표·인원수·간판 문구도 똑같은 주장이다(ADR-013). 생성 모델은 지시하지 않은 구체값을 스스로 채워 넣으므로, 개념 이미지라도 채택 전에 눈으로 확인하라.
- **부기 캐릭터는 현재 사용 보류다 (2026-08-03 사용자 결정).** 산출물에 넣지 마라. 아래 두 규칙은 보류가 해제될 경우에 대비해 남겨둔다.
- CRITICAL: 부기 라이선스가 승인완료가 아니면 대외 발행하지 마라. 내부 시안까지만 허용.
- CRITICAL: 부기를 **AI로 생성·변형하지 마라.** 좌우 반전·비율 왜곡·색 변경·새 포즈/표정/의상 생성 전부 금지다. 배경 제거와 업스케일만 허용. 등장 카드마다 저작권 표기 필수. 전체 규정은 `.claude/skills/sojourn-brand-system/references/boogie-usage.md`.
- `_workspace/`를 지우지 마라. 사후 검증과 부분 재실행의 근거다.

## 디렉토리

```
references/          원본 참고 자산 (boogie.jpeg, reference1/2.jpeg, 부기 공식 가이드북 PDF 2종)
assets/boogie/       부기 누끼 PNG (1회 준비)
_workspace/          중간 산출물 — 보존
output/              최종 산출물
.claude/agents/      에이전트 5인
.claude/skills/      스킬 8개
web/                 로컬 캠페인 콘솔 (Agent SDK). 의존성은 여기 격리 — 루트 금지
```
