node_modules/
.next/
out/
next-env.d.ts
tsconfig.tsbuildinfo

# phase execution outputs
phases/**/phase*-output.json
phases/**/step*-output.json

# 부산시 부기 공식 가이드북 — 로컬 보관만.
# 가이드북 본문에 "외부에 무단 유출, 복제할 수 없습니다"가 명시되어 있어
# 저장소에 두지 않는다. 추출된 규정은 아래 파일에 전부 옮겨져 있다:
#   .claude/skills/sojourn-brand-system/references/boogie-usage.md
references/*.pdf

# 브라우저 자동화 세션 아티팩트 (스냅샷·콘솔 로그). 산출물이 아니다.
.playwright-mcp/

# macOS
.DS_Store

# 환경변수 — 값이 비어 있어도 추적하지 않는다 (향후 API 키 유출 방지)
.env
.env.*
!.env.example

# 웹 콘솔 — 실행 이력과 원장 백업은 로컬 운영 데이터다. 산출물이 아니다.
web/.runs/
web/.brief-backups/
