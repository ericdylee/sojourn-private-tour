# PRD: Sojourn Korea 부산 프라이빗 투어 마케팅

## 목표

부산 프라이빗 투어 랜딩(`https://www.sojournkorea.net/private-tour`)으로 가는 트래픽과 예약 문의를, 캠페인 1회분 콘텐츠 세트를 반복 생산해 만든다.

성공 기준은 콘텐츠 생산량이 아니라 **랜딩 유입과 문의 폼 제출**이다. 조회수가 올라도 문의가 안 늘면 앵글이 틀린 것이다.

## 사용자

**Sojourn Korea 마케팅 담당 1인.** 디자이너·카피라이터·영상 편집자를 따로 두지 않고 혼자 캠페인을 돌린다.

이 제약이 설계 전체를 규정한다:
- 사람이 매번 판단해야 하는 지점이 많으면 운영이 안 된다 → 브랜드 규칙을 문서와 코드로 고정한다
- 산출물 검수를 사람 눈에만 맡길 수 없다 → 기계가 잡을 수 있는 것은 렌더 단계에서 잡는다
- 한 번 발행하면 회수가 어렵다 → 팩트·링크·라이선스는 발행 전 게이트로 막는다

## 핵심 기능

1. **캠페인 브리프** — 소재 하나 → 페르소나·훅 앵글·카드 6장 스켈레톤·SEO 키워드·팩트 원장. 뒤의 모든 산출물이 여기서 파생된다.
2. **카드뉴스 6장** — 1080×1080 영문. HTML/CSS로 조판하고 Playwright로 PNG를 뽑는다.
3. **블로그 3,000자** — 자사 사이트 게재, 구글 SEO. 메타·FAQ 스키마·내부 링크 포함.
4. **SNS 3편** — Threads / LinkedIn / X. 같은 메시지를 채널 문법에 맞게 각각 다르게 쓴다.
5. **브랜드 QA** — 발행 전 교차 검수. 렌더 이미지를 실제로 열어 확인하고, 팩트를 원장과 대조하고, PASS/HOLD를 판정한다.
6. **인스타 릴스** — QA를 통과한 카드 6장을 9:16으로 재활용.

## MVP 제외 사항

- **자동 발행.** 각 채널 API 토큰·비즈니스 계정 연동이 선행되어야 하고, 승인 없이 나가는 발행 사고를 막는 장치도 따로 설계해야 한다. 파일 산출까지만 한다.
- **서울 코스.** 부산에 집중한다. 랜딩에 서울 3코스가 있지만 이번 캠페인 대상이 아니다.
- **한국어판.** 사이트가 전면 영문이라 랜딩 경험과 끊긴다. 영어 단독으로 간다.
- **가격 소구.** 랜딩에 가격 표기가 없어 대조할 원본이 없다. 공식 가격표가 나오기 전까지 전 산출물에서 다루지 않는다.
- **고객 후기 카드.** 검증된 후기가 없다. 지어내지 않고 팩트 카드로 대체한다.
- **네이버 블로그.** 구글 SEO 한 곳에 집중한다.

## 디자인

- **초대형 볼드 타이포가 주인공.** 헤드라인 길이가 곧 레이아웃이다.
- **컬러 2개 + 극단 대비.** 브랜드 블루 + 액센트 1종.
- **부산시 캐릭터 부기를 화자로.** 장식이 아니라 부산을 대신 말하는 역할. 단 **부산시 서면 승인 전까지 대외 발행 금지.**
- 레퍼런스: `references/reference1.jpeg`(콜라주·아웃라인 타이포), `references/reference2.jpeg`(초대형 타이포·3D 마스코트 합성)

상세는 `docs/UI_GUIDE.md`, 실행 규칙은 `.claude/skills/sojourn-brand-system/SKILL.md`.

## 현재 상태 (2026-08-08)

새 세션은 이 절부터 읽어라. 진행 상황의 단일 출처다.

| 항목 | 상태 |
|------|------|
| 하네스 (에이전트 5 + 스킬 8) | 구축 완료. **2026-08-05에 `campaign-strategist` 제거 — 6인 → 5인** |
| **팩트 원장 소유권** | **사람.** `_workspace/01_brief.json`을 쓰는 에이전트는 없다. 콘솔 브리프 화면에서 만들고 편집한다. 에이전트는 읽고·대조하고·지적만 한다. 규율은 `campaign-brief` 스킬 |
| **웹 콘솔 (`web/`)** | **구축·검증 완료.** `cd web && npm start` → `http://127.0.0.1:4173`. Claude Agent SDK를 `settingSources:['project']`로 띄워 하네스를 **복제하지 않고 그대로** 로드한다. 실행·라이브 타임라인·산출물 뷰어·팩트 원장 편집·이력/복원 5개 뷰. 이름은 **Sojourn 콘텐츠 자동화**. 실측 검증: 프로젝트 에이전트 5/5·스킬 8/8 로드, 승인 게이트 왕복, 원장 검증 4종, 중단, 스냅샷 복원 |
| 콘솔 실행 단위 | **한 번에 에이전트 한 명.** 전체 캠페인 일괄 실행 버튼은 의도적으로 없다 (사용자 결정 2026-08-05). 사람이 한 단계씩 판단해 연결한다. `busan-campaign` 오케스트레이터는 하네스에 남아 있어 지시문으로는 쓸 수 있다 |
| 렌더 파이프라인 | 검증 완료 — 규격·오버플로우·락업충돌·부기 라이선스·사진 출처 + **외관 2종(줄수·대비)** 자동 검사. 결함 재현 테스트로 확인 (ADR-014) |
| 사진 파이프라인 | 구축 완료 — 매니페스트 원장 + 트리트먼트 CSS + 출처 차단 (음성 테스트 4건 통과) |
| 랜딩 URL | 확정 — `https://www.sojournkorea.net/private-tour` (200) |
| 카드 세트 v1 | 6장. QA 통과본. 타입 중심, 이미지 2/6. `_workspace/cards_v1/` + `03_cards_prev.html` |
| 카드 세트 v2 | reference2 룩. 두들 6/6, 이미지 4/6(콜라주). `_workspace/cards_v2/` + `03_cards_v2.html` |
| 카드 세트 v3 | 사진이 배경 전체. 풀블리드 사진 + 스크림 + 흰 웨이브 + 스파크. 이미지 6/6. `_workspace/cards_v3/` |
| **배경 구성 (ADR-015)** | 장소를 주장하는 카드만 실사진, 무드만 필요하면 gpt-image-2 허용(랜드마크 배제 조건부). **v4는 감천 단일 주제라 6장 전부 실사진이다** |
| v3의 알려진 약점 | 생성 무드컷이 **한국처럼 보이지 않는다.** 부산처럼 보이는 순간 ADR-010 위반이라 익명성이 사용 조건이다 |
| **카드 세트 v4 (현재 `output/cards/`)** | **SIMPLE · 감천 단일 주제.** 사진 풀블리드(원색) + 검정 스크림 + **Montserrat 800 2줄** + 하단 1줄 + 배지 + 세로 핸들. 카드마다 **핵심 구절 하나만 `--orange #FF7A29`**, 배지도 같은 오렌지. Anton·두들·웨이브 전부 미사용. **렌더 exit 0, 대비 경고 0** |
| ~~v4의 약한 고리~~ (해소) | 카드 05의 "It is **stairs**"가 사진과 어긋났다. 2026-08-06에 **"Even the easy way / is uphill"**로 교체 — 사진을 문구에 맞추는 대신 문구를 사진에 맞췄다. **감천 계단 사진 촬영 요청은 유효**하다(`manifest.wanted`) — 계단을 정면으로 다루는 카드를 만들려면 여전히 필요하다 |
| **v4의 발행 차단 사유** | **해소 (`0fb1aa3`, 2026-08-07).** 배경 사진 6슬롯 전량을 CC BY-SA 2.0에서 CC BY 4.0 / CC0로 교체 — 동일조건변경허락 의무가 사라졌다(실측: 현재 `03_cards.html`이 참조하는 6장 전부 SA 아님). 같은 커밋에서 QA 3회차 B1(카드 06의 미보증 드롭·픽업 문장)도 함께 교정됐다. **다만 이 교정은 QA 3회차 판정 이후에 들어갔다 — 공식 판정은 아직 그 3회차 HOLD 그대로다.** 재검수가 다음 작업 |
| 블로그 · SNS | 생산 완료 — `output/blog.md`, `output/social.json` |
| QA 1회차 (v1) | HOLD (BLOCKER 2 · MAJOR 5 · MINOR 4) — `output/qa_report.md` |
| QA 2회차 (v1) | **BLOCKER 0 · MAJOR 0.** 반려 7건 전부 픽셀 실측으로 해소 확인 |
| **블로그 형식 확정** | **한 편 = 한 장소.** 매거진 기사체 — 그 장소가 뭔지·간략한 역사·위치와 가는 법·추천. 후기체가 아니다 |
| **블로그 2편 발행** | **완료 — 라이브.** ① 해동용궁사 ② 감천문화마을. 각 Article + FAQPage, 소제목마다 사진 1장, sitemap 등재 |
| 블로그 목록 `/blog` | 가로형 카드(이미지 좌·텍스트 우) 세로 스택. **8편/쪽, 넘치면 `/blog/page/2`.** 테스트 9종 |
| 제목 줄바꿈 | 프론트매터 `title`에 `\|`를 넣으면 그 지점에서 끊긴다 (h1만, 1024px 이상) |
| 폐기 | 첫 글(여러 장소 물류 논증)과 추상 개념 히어로. 옛 URL은 301로 새 글에 연결 |
| SNS 3편 | 파일까지만. 발행은 사람이 한다 (ADR-009) |
| 부기 캐릭터 | **사용 보류 확정 (사용자 결정).** 산출물에 넣지 않는다. 라이선스 승인·고해상도 원본은 더 이상 대기 항목이 아니다 |
| 이미지 생성 경로 | gpt-image-2 (Codex 빌트인) 도입 — 크레딧 0. ADR-011 개정 |
| **릴스 파이프라인** | **구축 완료.** HTML 원본에서 9:16 재조판 → 결정론적 프레임 캡처 → ffmpeg. **크레딧 0.** 검사 8종(기존 4 + 세이프에어리어·단어수·3프레임 대비·text-transform). 발행 게이트는 fail-closed — QA PASS와 사진 권리를 둘 다 증명해야 배너 없는 `reel.mp4`가 나온다 |
| 릴스 첫 편 | `output/reels/reel_INTERNAL.mp4` (1080×1920·30fps·22.5초·675프레임, 실측). QA 재검수 전이라 배너가 굽혀 있다 |

### 다음 작업

> **세션을 시작하면 사용자에게 1·2번을 먼저 알려라.** 0번은 배경이다.
>
> 0. **웹 콘솔로 돌릴 수 있다.** 터미널 대신 `cd web && npm start` →
>    `http://127.0.0.1:4173`. 이름은 **Sojourn 콘텐츠 자동화**.
>    **한 번에 에이전트 한 명**을 골라 지시하는 방식이고, 에이전트마다 아래 대기
>    작업을 반영한 프리셋이 붙어 있다. 승인이 필요한 지점은 화면에 카드로 뜨고,
>    실행 전 `output/`·`_workspace/` 스냅샷이 자동으로 떠져 되돌릴 수 있다.
>    상세는 `web/README.md`.
>
> 1. **QA 재검수가 최우선이다 — 카드 반려가 교정됐는데 판정은 아직 HOLD다.**
>    `0fb1aa3`(2026-08-07)에서 QA 3회차 BLOCKER 3건 중 2건이 고쳐졌다 — 카드 06의
>    미보증 드롭·픽업 문장 교체(B1), 감천 사진 6슬롯 전량을 CC BY-SA 2.0에서
>    CC BY 4.0 / CC0로 교체(B2, 동일조건변경허락 의무 소멸). 하지만 이 교정은
>    **QA 3회차 판정 이후**에 들어갔고, 재검수는 아직 돌지 않았다. 릴스 발행 게이트도
>    같은 `output/qa_report.md`를 읽으므로, 재검수가 PASS로 안 바뀌면 카드도 릴스도
>    영구히 `_INTERNAL` 딱지를 단다.
>
> 2. **부산 루트 실사진 확보 — 특히 A5(전용 차량 실내)와 릴스용 영상 클립.**
>    라이선스 문제는 위 1번에서 해소됐지만, 상품 차별점(전용 차량)을 보여주는 이미지가
>    여전히 0장이다. `docs/SHOTLIST.md`에 **영상 클립** 절이 새로 생겼다 — 정지 사진을
>    찍는 자리에서 같은 구도로 5~10초 클립도 함께 찍으면 추가 비용 없이 릴스 포맷의
>    천장이 올라간다. 이건 사람이 찍어야 하고 에이전트가 못 푼다.
>
> 3. **다음 장소 편** (블로그 재개 시): ① 자갈치시장 ② 흰여울문화마을 ③ 해운대·스카이캡슐 ④ 태종대·송도.
>    감천 편(`2026-08-04-gamcheon-culture-village.md`)이 최신 형식 원본이다 —
>    도입 훅 → 역사 → 볼 것 → (필요시 별도 절) → 가는 법 → 언제 갈까 → How we help, 소제목마다 사진 1장.
>    콘솔 블로그 에이전트에 `자갈치시장 편` 프리셋이 있다.
>
> 카드 세트 이력: v4(현재) = `output/cards/` · v3 = `_workspace/cards_v3/` ·
> v2 = `_workspace/cards_v2/` · v1 = `_workspace/cards_v1/`. 되돌리려면 해당 html을 다시 렌더한다.

#### 콘솔에서 아직 실제로 안 해본 것 (2026-08-06 기준)

첫 실사용에서 드러날 수 있으니 새 세션은 이걸 알고 시작하라.

| 항목 | 상태 |
|---|---|
| 하네스 로드 (에이전트 5/5 · 스킬 8/8 · 게이트 연결 · 툴 35개) | 실측 확인 |
| 서브에이전트 위임 (리드 → `card-producer`) | 실측 확인 |
| `ask_choice` 게이트 | 실제 카드 작업에서 왕복 확인 |
| `ask_approval` 게이트 | **실제 에이전트 호출 확인** (2026-08-06 카드 05 수정). 단 아래 결함 ① 참조 |
| `ask_text` 게이트 | **실제 에이전트 호출 확인** (2026-08-06 QA가 카드 06 서비스 약속을 사람에게 질의) |
| 카드 부분 수정 + 재렌더 | **실측 확인** — 05만 변경, 나머지 5장 바이트 동일을 sha256으로 검증 |
| 카드 6장 신규 제작(1단계부터) | **미실행.** 프리셋만 준비됨 |
| 릴스 에이전트 (콘솔에서) | **미실행.** 파이프라인 자체는 구축·검증 완료(터미널에서 실행) — 콘솔 UI를 통한 실행만 아직 안 해봤다 |
| 원장 생성·편집·검증, 스냅샷 복원, 중단 | 실측 확인 |

**첫 실사용에서 나온 콘솔 결함 2건 (2026-08-06, 미수정):**

① **승인 카드의 버튼이 화면 밖으로 밀린다.** 에이전트가 근거를 길게 쓰면 승인/반려
버튼이 스크롤 위로 사라져 버튼이 있는지조차 안 보인다. 버튼을 카드 안에 고정하거나
상단으로 올려야 한다.

② **탭을 닫으면 진행 중인 실행과 화면이 끊긴다.** 서버는 게이트를 열어둔 채 정상
대기하는데, 새 탭으로 다시 들어가 누른 승인이 먹지 않았다. 결국 `POST /api/gates/:id`로
직접 응답해 풀었다. **앱으로 포장하면 창 닫기가 더 흔해지므로 재접속 복구가 필요하다.**

**사이트 소스 레포: `~/PROJECTS/sojourn-relocation-v1`** (remote는 `sojourn`, `origin`이 아니다 —
`origin`은 무관한 harness_framework를 가리킨다). 발행은 `content/posts/YYYY-MM-DD-slug.md` 작성 후
`npm run publish:blog`. `faq:` 프론트매터를 넣으면 FAQPage 구조화 데이터가 함께 나가고,
**모든 답변이 본문에 그대로 있어야 빌드가 통과한다.**

남은 MINOR: 블로그 내부 링크 1회 (사이트 URL이 4개뿐이라 링크할 관련 서비스 페이지가
아직 없다). v4는 6장 전부 사진이 차 있으므로 "빈 사진 슬롯" 항목은 해소됐다.

작업 후 이 절을 갱신하고 `CLAUDE.md` 변경 이력에 기록한다.

### 사용자 액션 대기 (에이전트가 못 하는 것)

1. **SNS 3편 발행** — `output/social.json`. 자동 발행은 MVP 제외라 사람이 올린다 (ADR-009)
2. **부산 루트 실사진 + 릴스용 영상 클립** — `docs/SHOTLIST.md` 우선순위 A 5컷 + 새 "영상 클립" 절.
   **A5(전용 차량 실내)가 최우선** — 상품 차별점을 보여주는 유일한 컷인데 지금 없다

부기 관련 대기 항목(라이선스 승인 신청·고해상도 원본)은 **사용 보류 확정으로 해제됐다.**
보류가 풀리면 `.claude/skills/sojourn-brand-system/references/boogie-usage.md`부터 다시 읽어라.

### 제약

- **Higgsfield 잔액 8 크레딧, 무료 플랜.** 카드 이미지는 gpt-image-2, 릴스는 CSS+Playwright+ffmpeg으로 가므로 **현재 파이프라인은 어느 쪽도 이 잔액을 쓰지 않는다.** 남아 있는 이유는 순전히 기록용 — 향후 실제 영상 생성 모델을 다시 쓰기로 결정하면 그때 `get_cost: true`로 preflight하고 사용자 확인을 받아라.
- **`slot: "place"`는 여전히 AI로 채울 수 없다.** gpt-image-2가 붙었어도 매니페스트 `wanted` 5건
  (감천·자갈치·용궁사·광안리·전용 차량 실내)은 전부 실사진 대상이다. 렌더러가 차단한다(ADR-010).
- 부기 가이드북 PDF 2권은 "무단 유출·복제 금지" 문구 때문에 `.gitignore`로 제외하고 로컬 보관만 한다.
  추출된 규정은 `.claude/skills/sojourn-brand-system/references/boogie-usage.md`에 전부 옮겨져 있다.
- **`_workspace/02_carousel.json`이 낡았다 — 고치지 마라, 이건 card-producer 몫이다.** QA 3회차 교정(`0fb1aa3`)이
  `03_cards.html`에는 들어갔지만 캐러셀 원장에는 되돌려 쓰이지 않았다. 카드 06의 미보증 드롭·픽업 문장과
  카드 03 헤드라인의 `directly` 누락이 원장에 그대로 남아 있다. 릴스는 이 원장이 아니라 `03_cards.html`을
  직접 읽어 이 드리프트를 우회했다. 다음 card-producer 세션이 원장을 `03_cards.html`과 재동기화해야 한다.
- **`assets/photos/manifest.json`에 고아 CC BY-SA 2.0 행 5건이 남아 있다 (`place/gamcheon-hero-src` 등).**
  2026-08-07 사진 교체 때 빠진 잔재이고, 현재 카드 세트는 이 중 무엇도 참조하지 않는다. 지금은 무해하다 —
  렌더러가 실제로 쓰인 사진만 라이선스를 검사하고 SA는 게이트가 막는다. 하지만 지우지 마라 — 사진
  원장은 사람 소유이고 소유자가 아직 판정하지 않았다. 다음에 이 행 중 하나를 카드가 다시 집으면
  이미 해소됐다고 기록된 발행 차단 사유가 조용히 되살아난다는 점만 기억하라.
